/** Compatibility entry point for analyzer and worker contracts. */
export type {
  CandidateAnnotation,
  CandidateSource,
  DefaultEntityClass,
  EntityClass,
  EntityIdPolicy,
  IdType,
  PossibleIdType,
} from "./types";
